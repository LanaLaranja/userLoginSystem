package com.example.demo;

import org.junit.jupiter.api.Test;

/**
 * Placeholder test kept in the original folder.
 * This test intentionally does not load the Spring context to avoid
 * duplicate configuration lookups — the real context test lives in
 * `com.cloudtechsolutions.wiki.DemoApplicationTests`.
 */
class DemoApplicationTests {

	@Test
	void placeholder() {
		// noop
	}

}
